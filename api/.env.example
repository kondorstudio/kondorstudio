##############################################
# KONDOR STUDIO — API (.env.example)
# Copiar para .env e preencher os valores.
##############################################

##############################
# 🌍 AMBIENTE / SERVIDOR
##############################
NODE_ENV=development
PORT=4000

##############################
# 🗄️ BANCO DE DADOS
##############################
# Exemplo para Render PostgreSQL:
# postgres://user:pass@host:5432/dbname?sslmode=require
DATABASE_URL=

##############################
# 🔥 REDIS / FILAS
##############################
REDIS_URL=

# ===============================
# WORKERS / JOBS (FASE 4)
# ===============================

# Intervalo de polling de alguns workers legados baseados em setTimeout
WORKER_POLL_INTERVAL=5000

# Número máximo de tentativas antes de marcar um job como failed
WORKER_MAX_ATTEMPTS=5

# Período de agregação de métricas (ms)
METRICS_AGG_PERIOD_MS=3600000

# Período de geração de relatórios (ms)
REPORTS_GENERATION_PERIOD_MS=900000

# Período de automação de WhatsApp (ms)
WHATSAPP_AUTOMATION_PERIOD_MS=300000

# Backoff padrão para jobs de métricas (ms)
METRICS_BACKOFF_MS=60000

# Backoff padrão para jobs de relatório (ms)
REPORT_BACKOFF_MS=60000

# Backoff fixo opcional para WhatsApp (ms). Se vazio, usa backoff exponencial.
WHATSAPP_BACKOFF_MS=

##############################
# 🔐 JWT / AUTENTICAÇÃO
##############################
JWT_SECRET=
JWT_REFRESH_SECRET=
JWT_EXPIRES_IN=7d
REFRESH_TOKEN_EXPIRES_IN=30d

##############################
# 🔐 CRIPTOGRAFIA (TOKENS)
##############################
# Chave forte (mín 32 chars) usada para criptografar tokens no banco (AES-256-GCM).
# Não coloque valor real aqui; defina no ambiente (.env/Render).
CRYPTO_KEY=

##############################
# 🌐 CORS / URLS PÚBLICAS
##############################
# Lista separada por vírgula (SEM ESPAÇOS)
# Exemplo: http://localhost:5173,http://localhost:4173
CORS_ORIGIN=
PUBLIC_APP_URL=
APP_PUBLIC_URL=
APPROVAL_PUBLIC_LINK_TTL_HOURS=72

##############################
# 📦 S3 / STORAGE
##############################
S3_ENDPOINT=
S3_BUCKET=
S3_ACCESS_KEY_ID=
S3_SECRET_ACCESS_KEY=

##############################
# 💬 WHATSAPP PROVIDER
##############################
WHATSAPP_API_URL=
WHATSAPP_API_KEY=
WHATSAPP_FROM_NUMBER=
WHATSAPP_TIMEOUT_MS=8000
WHATSAPP_PROVIDER=generic
WHATSAPP_TOKEN=
WHATSAPP_VERIFY_TOKEN=
WHATSAPP_GRAPH_API_VERSION=v17.0

##############################
# 💬 WHATSAPP — CLOUD API (META) (webhook + respostas diretas)
##############################
WHATSAPP_META_TOKEN=
WHATSAPP_META_PHONE_NUMBER_ID=
WHATSAPP_WABA_ID=

##############################
# 📊 META / FACEBOOK ADS
##############################
META_GRAPH_BASE_URL=https://graph.facebook.com/v17.0
META_DEFAULT_FIELDS=impressions,clicks,spend
META_APP_ID=
META_APP_SECRET=
META_OAUTH_VERSION=v24.0
META_OAUTH_REDIRECT_URI=
META_SOCIAL_REDIRECT_URI=
META_ADS_REDIRECT_URI=
META_SOCIAL_SCOPES=
META_ADS_SCOPES=

##############################
# 📈 GOOGLE ADS
##############################
GOOGLE_ADS_API_BASE_URL=https://googleads.googleapis.com/v14
GOOGLE_ADS_DEFAULT_FIELDS=metrics.impressions,metrics.clicks,metrics.cost_micros
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

##############################
# 📝 AUDIT LOG (FASE 5)
##############################
AUDIT_LOG_ENABLED=false
AUDITLOG_SKIP_REGEX=^/health(z)?$|^/health$|^/api/auth
AUDITLOG_BODY_MAX=2000

##############################
# 💳 CHECK SUBSCRIPTION (FASE 5)
##############################
CHECK_SUBSCRIPTION_ENABLED=true

##############################
# 📈 TIKTOK
##############################
TIKTOK_CLIENT_KEY=
TIKTOK_CLIENT_SECRET=
